import type React from "react"
import { BookManager } from "../components/book/BookManager"

const Book: React.FC = () => {
  return (
    <div className=" ">
      <BookManager />
    </div>
  )
}

export default Book;